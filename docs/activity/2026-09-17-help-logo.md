# The main help message opens with the logo

The first dockhand opened its main help message with a text logo. It is now the first thing `dockhand --help` prints again, above the existing description, taken verbatim from the v1 source in `internal/cli/cli.go` with its trailing spaces intact. Subcommand help is unchanged, and the root CLI tests pass.
