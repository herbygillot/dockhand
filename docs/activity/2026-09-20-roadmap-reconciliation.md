# Reconciling the roadmap with two days of work

The roadmap said it was last reconciled on 2026-09-17. Two days of work had landed since, and thirteen of the twenty-four activity reports written on 2026-09-18 and 2026-09-19 were referenced nowhere in it. That work did not come from the queue: it was release engineering, two whole-tree reviews triaged the day each was written, and defects that surfaced from using Dockhand on its own MacPorts submission.

**Next** is empty and now says so plainly, with the nearest candidates named in the order they are worth weighing: the instance-coordination note's two foundation pieces, a claim owner with a stable identity and a heartbeat keyed by database and repository; then the narrower reads, still behind measurement, and the dependent verification follow-up. An earlier draft of this reconciliation put two other items first, a per-image lock in `setup` and a host-level Tart capacity lease, on the strength of the design note's claim that they were races. Checking the code found both already handled since 2026-09-14 and described in that day's [report](2026-09-14-tart-setup.md); the note and this list are corrected.

Three rows joined completed capabilities: advisory port tests, with the required policy and the test timeout; build, packaging, and version identity, covering the vendored tree, its make targets and CI check, and the version stamped from a tag or linked in by a packager; and contribution naming, covering the rule that a contribution answers to its initiating port and to any target it carries, and the resolution of `.` and paths to the port they name. The command-interface row gained the attached command's narrative and `DOCKHAND_DB`, and source preparation gained the `Generated-By` trailer. Maintenance records the three flakes closed and the README's accuracy pass and rewrite. Nothing in the two days opened a new question: every item in Later and Needs design stands as it did, and the instance-coordination item added on 2026-09-18 is the only one of them that is new.

Two stale cross-references went with it. The maintenance section pointed at the PortIndex storage design as owning index consolidation "as Next item 2", which that document has said was implemented since 2026-09-16; it now points at the consolidation report. And `version.Info.String()` still repeats the revision when Go stamped a pseudo-version, which was checked rather than assumed before it was kept on the list; a tagged build does not show it.
