# Reconciling the roadmap with the day's work

The second reconciliation of 2026-09-20, covering the day itself rather than the two before it.

Two rows joined completed capabilities. GitHub verification refinements, which came off Later entirely: the provider stopped pattern-matching `main.yml` and now judges the run it produced, a finished failure is run again rather than adopted, and a correction authorizes replacing the commit it corrects. And retrying stopped work, which turned out to be a capability that already existed and had never been described: submitting the same preparation again adopts the stopped job's branch and carries its publication forward, so the work was making the advice and the live table's key name that command instead of a fixed verb. The build and version row gained macOS 27 and the pseudo-version fix, along with the point that a release entering the table becomes selectable rather than adopted, since the release built by default is Tart's policy and stays Tahoe.

The header and Next section now say three days rather than two, and name what the day actually was: one Later item taken to completion because a retry could not work without it, and a design note's claims corrected.

That correction is recorded twice on purpose. The Needs-design item for instance coordination says which two of its claims were untrue and that they were found by reading the code rather than by inferring from a search, and the triage section adds the general form: a claim in a design note is evidence to check, the same way this document already treats a review's claims. The answer had been sitting in a report from 2026-09-14 the whole time, which is the part worth not forgetting.
