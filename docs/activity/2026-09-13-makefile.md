# Simple Makefile — 2026-09-13

Added a Makefile with a default build target producing `./dh2`, plus `test`, `test-race`, `vet`, and `clean`. `GO` and `BINARY` can be overridden on the make command line. Added the default build output to `.gitignore` and documented the commands in the README.

Validated real build and clean targets using a temporary output path, ran the built CLI's help, and checked default/test/vet recipe expansion with `make -n`. No application code, dependencies, or tests changed. The existing local `dh2` executable was preserved. All additions were authored for v2; nothing was copied from v1. Changes remain uncommitted.
