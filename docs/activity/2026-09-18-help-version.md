# The version under the logo

The main help message now prints `version <ver>` directly beneath the logo, using the same build description `--version` prints: the module version and short revision for an installed build, `devel` for a plain `go run`. The help test checks the line sits between the art's last row and the description.
