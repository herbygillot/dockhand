# Platform observations and context selection

Native Tcl observation now captures requested scalar writes and option return values with source frames. The evaluator reports facts; the editor owns boundary selection. Source scanning covers unvisited baseline and candidate branches, including inverted comparisons. A bounded fixed point adds both sides of resolved thresholds; changing or unobserved operands remain inconclusive.

Aliases and formatting reads use a bounded Darwin metadata enumeration (8 through the native major), retaining supported architecture choices. These are metadata observations, not build results. Unsupported minor-version/deployment dimensions and modeled host dependencies remain refusals. Baseline declaration observations are reused; final untraced candidate evaluations stay fresh.

Validation: native evaluator/editor suites passed. Authored integration cases cover scalar/option/inverted thresholds, aliases, formatting, mutable thresholds, revision reset, and preservation of independent older releases. Pinned real controls retain Wasmer/Terraform/Helm/gh/Deno behavior. py-openssl, mpir, and libfec now reach input-found assessment. mrustc resolves its option threshold but remains inconclusive because modeled evaluation accesses host state; this guard was deliberately retained. Whole-corpus comparison and session measurements accompany the complete coverage pass.

Code in this change was authored here; no v1 comments or tests were copied. Raw controls: `~/Documents/ChatGPT/Dockhand/exercises/2026-09-16-bump-coverage`.
