# The queue, settled

Next had carried candidates without an order since the 2026-09-18 queue drained. It is seven ordered items now, from the day's whole-tree survey and structural check, ranked by ports gained per unit of work with the two index-cache items last because a full index generation is paid rarely: a clean survey baseline with one JSON line per port, the pre-fetch guard grammar, an outcome for ports with nothing to fetch, literal segments of a composed version, host reads that decide only a note, seeding the index cache across registrations, and the mirror's index as a bootstrap seed. The coordination foundation pieces, the narrower state reads, and the dependent verification follow-up are carried unranked, and the human-edit capture decision stays under Needs design until it is made.
