--- stdout
dockhand/jq-queued               queued (Testos) — all 2 verification slots are busy (2 VMs running) — `dockhand cycle` starts it
dockhand/jq-local                passed (Testos)
discarded dockhand/jq-landed (8da38abbbd45)
dockhand/jq-landed               PR #80 merged — branch cleaned
dockhand/jq-open
  passed (Testos)
  PR #77 open (https://x/77)
dockhand/jq-running              verifying (1m30s) (Testos)
dockhand/jq-unasked              unverified — minted with --no-verify; `dockhand verify` starts a run
dockhand/jq-unreadable           error: reading the note: bad object
dockhand/jq-outage
  unverified
  PR state unavailable: gh api: HTTP 502 from api.github.com
erasure-test
  passed (Testos)
  PR #81 merged — not a dockhand branch, so nothing here removes it
dockhand/jq-held
  passed (Testos)
  PR #82 merged — kept: held (keeping it for a bisect, 2026-09-01 02:00 UTC)
--- stderr
removed dockhand/jq-landed from "herby"
dockhand/jq-held is held (keeping it for a bisect, 2026-09-01 02:00 UTC): the deletion is withheld — `dockhand unhold dockhand/jq-held` releases it
verify: submitted jq on Testos (job fake-1); `dockhand status` follows it
still deferred: dockhand/jq-local on Testos — all 2 verification slots are busy (2 VMs running)
