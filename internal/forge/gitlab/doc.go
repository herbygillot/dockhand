// Package gitlab adapts GitLab tag observations to forge contracts.
//
// It binds repositories on public or self-hosted instances and uses the GitLab SDK
// for exact-tag lookup and catalog pagination. MacPorts source interpretation and
// release selection remain with the consuming capability packages.
package gitlab
