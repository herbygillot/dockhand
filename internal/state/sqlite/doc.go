// Package sqlite implements Dockhand's state contracts with repository-scoped
// SQLite transactions. It validates durable relationships and lifecycle
// integrity while leaving workflow eligibility and policy to callers.
package sqlite
